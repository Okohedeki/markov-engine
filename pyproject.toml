[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "markov-engine"
version = "1.0.0"
description = "An evidence-first engine and delivery app for finished briefs, research reports, and factual scripts."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "markov-engine contributors" }]
keywords = ["knowledge-graph", "embeddings", "clustering", "anthropic", "agents", "rag"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "anthropic",
    "voyageai",
    "trafilatura",
    "yt-dlp",
    "pymupdf",
    "faster-whisper",
    "soundfile",
    "ddgs",
    "httpx",
    "pydantic",
    "pydantic-settings",
    "aiosqlite",
    "fastapi",
    "uvicorn",
]

[project.optional-dependencies]
# Run a model in-process from a GGUF file (LLM_BACKEND=llamacpp / EMBED_BACKEND=llamacpp).
local = ["llama-cpp-python"]

[project.urls]
Homepage = "https://github.com/Okohedeki/markov-engine"
Source = "https://github.com/Okohedeki/markov-engine"

[project.scripts]
markov = "markov_engine.cli:main"
markov-api = "markov_engine.api:main"

[tool.hatch.build.targets.wheel]
packages = ["markov_engine"]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"
